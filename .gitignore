# Secrets — never commit these
.env
*.env
!.env.example
config.local.toml
# Tu config local, copiada de config.example.toml. Sin secretos, pero
# versionarla haría que cada `git pull` de actualización choque con ella.
config.toml

# Runtime state
state.json
*.log

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.pytest_cache/
.coverage
htmlcov/
